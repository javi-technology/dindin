# Brapi API key — obter em https://brapi.dev
# Para deploy, usar: firebase functions:secrets:set BRAPI_API_KEY
# A key é enviada via header Authorization: Bearer (não via query string)
BRAPI_API_KEY=your_key_here
